# Origin of the Muse backend. Vite inlines this at BUILD time, so it must be set
# wherever the build runs (locally, or in the Vercel project's env settings).
# Leave unset for local development — the app falls back to http://localhost:8000.
VITE_API_ORIGIN=https://your-backend-host.example.com
